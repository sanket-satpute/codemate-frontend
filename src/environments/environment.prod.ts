export const environment = {
    production: true,
    // The backend deployed URL on Render or similar platform.
    // We use relative mapping so it honors the domain's /api route if proxied, or a hard URL if independent.
    // Modify this to match the actual deployed backend endpoint (e.g. https://codemate-backend.onrender.com/api)
    apiUrl: 'https://codemate-backend.onrender.com/api'
};
